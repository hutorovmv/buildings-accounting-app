@model IEnumerable<BuildingsAccounting.Web.Models.BuildingTypeTableModel>
@{
    ViewBag.Title = "Всі типи будівель";
}

<h2 class="mt-4 text-center">Всі типи будівель</h2>

<div class="container pb-4">
    <div class="row d-flex justify-content-start align-items-stretch">
        @foreach (var m in @Model)
        {
            @Html.Partial("_BuildingTypeCard", m)
        }
    </div>
</div>